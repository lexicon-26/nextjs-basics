- use shadcn UI + nextjs + base ui (to start a new project)
- npm run format (to format .tsx files in the project)
- npx shadcn@latest add --all (to add all shadcn components -> components/ui/...)

- every page.tsx needs to return react component in nextjs, otherwise generates error

- metadata inside root layout.tsx (import metadata from 'next')

- use <Link> to navigate between pages
- use buttonVariants/shadcn to show links as buttonVariants

- install prisma 7 (install dependencies section)
- npm install prisma@prev tsx @types/pg --save-dev
- npm install @prisma/client@7 @prisma/adapter-pg dotenv pg
- npx prisma init --output ../app/generated/prisma
    -- npx prisma init --output ../generated/prisma (if do not want to install inside app/)
- run npx prisma generate (it will create a generated folder in root)
- make changes to .env 
    -- DATABASE_URL=postgres://postgres:password@localhost:5432/db_name_here
- inside root lib folder, create 'prisma.ts' prisma client, copy the code to write in this file from prisma website under section 'lib/prisma.ts'

- create models inside (prisma/schema) file
- to create database and tables run
    -- npx prisma migrate dev --name init
    -- npx prisma generate (to generate new client)


- Server and Client Side Validations
- Server Actions (search) from client to Server

- Creating client side forms
    -- build forms in React using TanStack Form and Zod
    -- if you're already using React + TypeScript, then TanStack Form Zod is one of the best combinations today
        --- TanStack Form → handles form state, touched fields, submission, errors, performance.
        --- Zod → validates and type-checks your form data.
        --- Together they give you type-safe forms with minimal re-renders.

- Zod (used only for validation, means whatever data is put in the form, verify it)
    -- install 
        --- npm install zod @tanStack/react-form
    -- create a zod schema (means setting the rules)
    -- create the form
    -- connect the fields
    -- reusable zod validator
    -- validate entire form on submit

- z.schema() -> set the rules
- z.infer() -> create the TypeScript type from the z.schema() rules

- tanStack form validates the form
    -- a state mananger that checks what data user is entering, what is the current state of form and how it will be submit
    -- tanStack checks that once form is filled, is it according to the rules that zod has set
- zod validates the data
    -- a validation library, that verifies data, like password must be 6 characters
    -- zod create rules how data should be looked

- server actions and mutation
    -- server actions -> function that runs on server and can be called from browser
    -- mutation -> create, update, delete data
    -- query -> read data